# API URL (optional - auto-detected from window.location if not set)
# For local development: http://localhost:8090/api/v1
# For production: set to your server's API URL
# Leave empty to auto-detect based on current host
VITE_API_URL=
